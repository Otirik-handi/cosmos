---
parent: PROJECT-STATUS.md
range: 2026-09-15 ~ 2026-09-11(2 节)
tags: [project-status, history, governance]
tokens_est: 3400
---

## 2026-09-15：Phase 2 收口决定与 G 系列暂停

基线：`master` = `origin/master` = `e176312`，工作树干净、无遗留 worktree。本节记录维护者 2026-09-15 的 Phase 2 收口决定；治理系列的实测数据只在 [`.agents/tasks/governance/`](../.agents/tasks/governance/README.md) 维护，本节只记结论与链接。

**ORG-021 阶段归属更正**：维护者裁定 [PRD](../docs/requirements/0002-product-requirements.md) §7.5 ORG-021（Entry → Story 组织允许确定性算法、传统模型与 LLM 协同）的阶段列由 `Phase 2` 更正为 `Phase 3`。理由是它依赖 Agent 调用边界、Agent 候选 Revision 与确认策略、第一版预算模型，三者都是 Phase 3 交付物，且 §12 Phase 3 范围已含 Knowledge Workflow。原表与 §7.5 各切片注记「自动聚类与 Knowledge Workflow（ORG-021）后置」自相矛盾，并让 Phase 2 按需求表字面无法完成。更正登记在 PRD 主文档「分册勘误登记」（分册封口后只读），ORG-021 的需求文字、验收条件与既有切片注记均不改写。

**Phase 2 尾巴范围（维护者 2026-09-15 选定）**：

- 补做：Story split 的用户状态迁移与撤销（ORG-020 的 split 侧，[待决定事项 10](../docs/requirements/0002-product-requirements.md)）；看板 UI 缺口（多个 Feed Block 独立取数 + 区块拖拽排序）；四条主流程（Topic / Entity / 用户组织 / 看板）的真人验收；`PROJECT-STATUS.md` 状态补记。
- 本次不补、记为未关闭项：Entity merge/dedup、批注字符级锚点、`e2e/browser/ingest.spec.ts` 的 390px 溢出断言误报修复、`size-governance.py --check` 的行数阈值（G 系列暂停时留下的门禁欠账）。
- 不属于 Phase 2 尾巴：Phase 1 后置债（Docker/Compose、发布部署、真实公网长时定时抓取、非 Windows smoke、长时间故障恢复）仍按 2026-09-07 划线保留。

**Phase 2 四条验收标准的成立状态**：分类/Topic 浏览、多来源 Story 时间线与相关内容、可配置看板与删除 Block 不删底层信息三条有实现与浏览器自动化证据（见 [2026-09-09 节](history-2026-09-2.md)）。第四条「重分析不覆盖用户批注和人工关系修正」目前只靠"Phase 2 尚无自动重分析写入路径"结构上成立，未被真正考验；Phase 3 的 Knowledge Workflow 落地后需要重新回归。

**本轮实际运行的验证**（2026-09-15，主工作区 `e176312` 干净）：`bun run typecheck` 全仓通过；`bun run test` 87 文件 / 516 用例全绿（未复现既有的 Windows SQLite `migrate deploy` 超时与 EBUSY 抖动）；`bun run docs:check` 618 文件 failures=[]。未运行：property、e2e、浏览器、build、Windows Node smoke、Docker/Compose、发布部署。

## 2026-09-14 ~ 2026-09-11：G 系列代码与文档治理（G01–G07）

维护者 2026-09-11 为文档/代码规模治理设立独立编号体系（`G{NN}`，不占产品 Task 编号），章程与逐任务记录见 [`.agents/tasks/governance/README.md`](../.agents/tasks/governance/README.md)。七个对象全部收口，记录与代码均在 `master` 与 `origin/master`（`git merge-base --is-ancestor` 已核对 `4a29060`、`c89fe3e`、`66862ff`、`df76e96`、`06065cd` 均为两处祖先）。

| 任务 | 对象 | 结果 | 合入 |
| --- | --- | --- | --- |
| G01 | 文档治理首批（4 份存量超标文档） | 2026-09-11 收口 | 主工作区直接进行 |
| G02 | `packages/storage-prisma` | 入口 12 行门面、读取量 9 万 → 约 1.1 万 token | `4a29060` |
| G03 | `apps/api/src/app.controller.ts` | 按资源拆为继承链 + 231 字节门面（114 条路由快照冻结） | `c89fe3e` 等 |
| G04 | 文档治理第二批（PRD、信息模型、Task 02 README、product-dtos） | 主文档合计 271.7 KB → 50.2 KB，18 分册全部落健康区，文档基线 12 → 8 条 | 主工作区直接进行 |
| G05 | `packages/application` | 入口 2149 → 98 行，导出面 147 逐字节零 diff，循环依赖 3 → 0，读取量 −93% | `66862ff` |
| G06 | `packages/contracts` 入口、`apps/web` 的 `page.tsx` 与 `story-panel.tsx` | 1455 → 168 / 1680 → 738 / 1902 → 757 行；代码基线 18 → 8 条 | `df76e96` |
| G07 | `packages/transport-http` | `index.ts` 1269 → 11 行、`index.test.ts` 964 行 → 6 个域文件；导出面 4 个逐字节零 diff；代码基线 8 → 6 条 | `06065cd` |

- 行为等价底线在每次收口时于 `master` 重跑：导出面逐字节零 diff + 现有测试全绿 + 入口契约测试。G07 收口后为 typecheck 0、unit 87 文件·516 用例、property 4、e2e 4、浏览器 17/17、两份体积门禁 PASS、`docs:check` 618 文件 0 失败。
- 代价：模块数增加带来产物体积上升（`transport-http` 的 `dist` 94 → 186 KB；`application` 的 `dist` 735 → 953 KB）。这是文件固定开销，不是功能变化。
- CI：G05 推送首次触发远端 CI，`Node process E2E` 因既有 `e2e/scheduling.e2e.test.ts` 日志等待超时（31.8s > 30s 预算）失败，只重跑失败作业后 4 作业全绿，判定为既有用例时序 flake。G07 推送后 4 作业全绿。
- **G 系列暂停（2026-09-14，维护者）**：不做非红线文件。暂停时的两笔未结账：一是 `size-governance.py --check` 只判字节/token，行数越界不拦，而维护者已裁定采用完整口径（行数、字节、token 先到先触发），完整口径下仍有 8 个文件超红线（最大 `packages/application/src/workflow-host-runtime.ts` 1205 行）；二是 `e2e/browser/ingest.spec.ts:127` 的 390px 横向溢出断言已在 CI 两次误报（重跑即过、本地多次全绿），记录建议改为有界重试并在失败时打印最宽元素。恢复治理时按行数降序继续，选型须用 `--json` 自行核对 `lines` 字段。

## 附：被取代的 2026-08-24 快照（原文留存）

以下文字在 2026-09-15 之前是主文档「一句话结论」的正文，随该节刷新为当前快照而移出。

> Source 身份/revision 持久化合同与默认验收调用方已完成本地 clean cutover：Product API/Web/Node E2E/Windows smoke 使用 `sourceDefinitionRef + operationId + config` 创建默认停用 Source，再以 revision CAS activation command 启用；产品路径不再提交 `kind`、`enabled` 或 `fixturePath`。本轮经五轴审查收口：激活同键重放持久化并返回首结果快照（新 migration `20260824100000_source_activation_result_snapshot`）、API 边界配置校验切换 canonical Zod schema（Bilibili feed 缺 profile 即拒）、未启用 Source 手动 Run 返回 409、RSS `feedUrl` 收紧 http(s)、Idempotency-Key 统一 1–300 字符、迁移回归测试临时目录修复。本地通过全仓类型检查、34 文件/249 单元测试、4 文件/4 Node 进程 E2E、8 个生产浏览器场景、12 个组件实验室浏览器场景和 Windows Node smoke。Docker CLI 不可用，Docker 验收未运行；真实 RSS/AI HOT/Bilibili、断网媒体验收和发布部署仍未完成（媒体边界 v1 的设计与实现已于 2026-09-04 合入 master，见下方“当前下一步”；未保存配置 Probe 与 schema 驱动 Web 配置流程已分别于 2026-09-02 以 PR #1、PR #2 合入；两块固定看板与来源健康切片已于 2026-09-03 快进合入 master）。

**原文中与后续事实不符、已在主文档修正的部分**：Bilibili 已于 2026-09-04 通过真实数据 E2E（Run 成功、`itemCount=20`），AI HOT 真实 GET 已返回 200，真实单次与真实双源联网验收已过；「未完成」只成立于「真实公网长时定时抓取」与「断网媒体验收」的长期稳定性口径。原文的 34 文件/249 用例是 2026-08-24 当日基线，当前为 87 文件/516 用例。
