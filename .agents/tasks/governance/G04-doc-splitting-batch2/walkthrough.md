# G04 过程记录(append-only)

本文件是 G04 的过程、偏差与验证的唯一记录位置;README 只维护当前摘要、范围、门禁与下一步。追加规则与文档大小治理见根 `AGENTS.md`。

## 2026-09-14 切片 0:口径裁定与提案并入

- 背景:G01 收口后,`docs/doc-governance/docs-baseline.json` 的红线区仍留 4 份文档。提案 §4.1 的「不拆」豁免与 §4.2 的「红线必须拆」在其中的 `docs/api/0003-product-dtos.md`(§4.1 点名不拆)、`docs/architecture/0002-information-model.md`(扫描结论「单一主题,内聚」)上直接冲突。裁定前确认:这 4 份文档在全仓没有任何 `#` 锚点引用,仅按路径被引用(101 处),因此**只要主文档路径不变,引用不受拆分影响**。
- 裁定(维护者按 Agent 建议):四份全部判拆,无一走豁免。`0003-product-dtos.md` 按域分册;`0002-information-model.md` 按广域设计分册;`.agents/tasks/02-rss-ingestion/README.md` 的追加 slice 记录移入分册;`0002-product-requirements.md` 按领域分册。
- 提案并入:§4.1 增加「红线优先(2026-09-14 裁定)」段,并修正原「不拆」表述为「单一主题内聚**且已定稿冻结**」「**未触犯 4.2 红线**的按需查阅型参考清单」;§8 决策记录追加 2026-09-14 行(该行与既有的 §4.10 门禁口径行并列,两行日期相同但内容独立)。
- 偏差:首次编辑 §8 时用「被并入行的前缀」作为锚点做替换,把既有 2026-09-14 门禁口径行截断并与新行合并成一行(该行长度由 548 字符变为 905 字符)。发现后按边界串拆回两行并补回被截断行的前缀,复核:文件内 `| 2026-09-14 |` 行数为 2、末行结构正常、文件 18,633 字节仍在健康区。教训:以行首片段作锚点时必须确认锚点唯一且不吞掉同前缀行,本次两行前缀完全相同(`| 2026-09-14 | **§4.…`),属该类风险。
- 验证:`python scripts/size-governance.py -c docs --check --baseline docs/doc-governance/docs-baseline.json --fail-on-new` 仍 PASS(提案与 G04 文档均在健康区);`bun run docs:check` 0 失败(517 文件)。
- 未运行:拆分实施的守恒校验与门禁复跑(切片 1–5)。

## 2026-09-14 切片 1:PRD 0002 按领域分册(7 册)

- 原 99.19 KB / 897 行 / ~33,343 token → 主文档 14.9 KB / ~5,076 token(保留 §0–§5 概览与原则、§13 待决定事项、分册索引与原位链接行)+ 7 个分册:`part-06`(4.4 KB)、`part-07-1`(18.4 KB)、`part-07-2`(21.4 KB)、`part-07-3`(15.1 KB)、`part-08-09`(5.9 KB)、`part-10-12`(9.6 KB)、`part-14-15`(12.8 KB)。
- §7 功能需求单章 56.0 KB,**超过 §4.4 的 50 KB 单册硬上限**,故按其 `###` 子节再下钻三册:§7.1–7.4、§7.5(21.2 KB,Story/Topic/Entity 与关系)、§7.7–7.11。
- 偏差:原文 §7 的编号从 7.5 直接跳到 7.7,没有 §7.6(既有编号缺口,非本次引入)。零丢失纪律要求保留原样,未做重编号。
- 验证:标题多重集守恒(丢失 0 / 新增 0,新增仅「## 分册索引」一节);行级多重集守恒(缺失 0);分册正文经链接深度回退后是原文逐字子串。

## 2026-09-14 切片 2:信息模型 0002 按广域设计分册(3 册)

- 原 61.0 KB / 1047 行 / ~20,223 token → 主文档 9.8 KB / ~3,284 token(保留 §1 本轮结论、§11 架构不变量、索引与链接行)+ 3 个分册:`part-02-04`(19.3 KB)、`part-05-10`(23.5 KB)、`part-12-14`(10.1 KB)。
- 按提案 §4.3「广域设计 → 主文档留索引 + 架构不变量 + 当前决定」取舍:§1「本轮结论」是当前决定、§11「架构不变量」是跨模块约束,两者留主文档;§14 变更记录随 §12–13 归档,后续变更条目追加于主文档索引节之后。
- 验证同上;分册 `part-05-10` 为本次最大分册(23.5 KB / 7,734 token),仍在健康区。

## 2026-09-14 切片 3:02-rss-ingestion README 的追加 slice 记录归档(3 册)

- 原 57.4 KB / 439 行 / ~18,561 token → 主文档 22.4 KB / ~7,165 token(任务模板全文 + 原位标题 + 索引 + 链接行)+ 3 个分册:`slices-2026-08-23-09-02`(18.3 KB)、`slices-2026-09-03`(10.0 KB)、`slices-2026-09-04-07`(8.6 KB)。
- 容器 `## Current implementation slice：配置优先的产品 E2E（2026-08-23）` 下的 23 行自述与「合同切换登记(2026-08-24)」属 2026-08-24 的历史登记,随分册归档;容器标题留在主文档原位作为阅读入口,分册正文从标题之后开始(标题不在两处重复,标题多重集守恒)。
- **偏差:计划 2 册改为 3 册。** 首轮把 6 个子节按 3+3 分两册,含容器自述后第一册达 28.2 KB / 9,166 token,**越过健康区的 9k token 线**(字节仍在 30 KB 内);改为「自述 + 前两个切片 / 单个 09-03 切片 / 后三个切片」三分,最大册降到 18.3 KB / 5,896 token。这也是本次唯一按 token 而非字节触发的重切。
- 验证同上。该 Task 的「Phase 1B：Collector Runtime」「Current State」「Implementation Walkthrough」「Verification」「Follow-ups」等模板节全部留在主文档,未被当作历史切走。

## 2026-09-14 切片 4:product-dtos 0003 按域分册(5 册)

- 原 54.2 KB / 2124 行 / ~14,166 token → 主文档 3.1 KB / ~943 token(只留 §1 规则 + 索引 + 链接行,符合 §4.3「参考清单 → 主文档只留使用说明和索引」)+ 5 个分册:`part-02-04`(13.1 KB)、`part-05-06`(10.9 KB)、`part-07-09`(10.6 KB)、`part-10-13`(8.3 KB)、`part-14-16`(10.9 KB)。
- 该文档是本次唯一「字节超红线但 token 未超」(14.2k < 15k)的样本,即提案 §4.2「先到先触发」中字节先到的案例;它也是 §4.1 原点名「不拆」、经 2026-09-14 裁定改判拆的那一份。
- 验证同上;该文档全文无相对链接(链接深度修正 0 处),拆分不涉及引用修复。

## 2026-09-14 切片 5:收口

- 基线:`docs/doc-governance/docs-baseline.json` 只删条目不增条目,12 → 8 条(移除本任务四份);`generated` 字段按既有做法保持 `2026-09-11`(历次条目缩减均未改该字段),缩减过程由 git 历史追溯。
- 门禁:`python scripts/size-governance.py -c docs --check --baseline docs/doc-governance/docs-baseline.json --fail-on-new` → PASS(保留 1 条 warning:`docs/spec/interfaces/0002-product-api-http.md` 41.99 KB > 登记 41.76 KB,与本次改动无关)。代码侧 `-c code tests`(code-baseline)复核未受影响 → PASS。
- 链接:`bun run docs:check` → 536 文件 0 失败。
- 变更卫生:`git diff --check` 干净(无尾随空白);变更集中在 7 个已跟踪文件与 18 个新分册,主文档合计 271.7 KB → 50.2 KB。
- 未运行:浏览器验收、真实来源验收(纯文档改动,不适用);CI 门禁(改动未提交、未推送,本地等价命令已跑)。

## 2026-09-14 过程偏差汇总(切片 1–4 共同)

- **占位行重复(已修)**:首版切分对每个被移走的 `##` 块各生成一行「已归档」链接,同一分册的相邻块因此产生 2~5 行完全相同的链接(dtos 主文档一度 15 行)。改为「相邻同分册的整块合并为一行」后,占位行为 PRD 7 行、信息模型 3 行、dtos 5 行、rss 3 行,与分册数一致。前两次写入均在发现后回滚(`git checkout` 原文 + 删除新分册目录)再重跑。
- **链接深度(已修,G01 同类问题重演)**:内容移入子目录后,原按主文档所在目录书写的相对链接少一层(如 `../proposals/x.md` 应为 `../../proposals/x.md`)。首版漏修,`docs:check` 报出失败。修复只改链接深度、不动正文,共 31 处(PRD 16、信息模型 13、rss 2、dtos 0),并在守恒校验前把深度改回来比对,确认正文逐字节未变。
- **修复本身的第一次尝试失败(偏差自省)**:我按「行内代码切段」实现链接识别,而检查器 `scripts/check-documentation.ts` 的做法是把行内代码替换成**等长空格**(保留位置)。链接文本含行内代码时(如 ``[`trigger-sdk-v1` Proposal](../proposals/trigger-sdk-v1.md)``),切段会把 `[` 与 `](...)` 分到两段,正则匹配不到、链接未修。改为同口径(掩码副本上定位目标、按位置修改原行)后修复生效。教训:凡是要与既有检查器对齐的解析,先读它的实现再写自己的。
- **分册大小口径(已修)**:索引表首版列的是分册正文字节,不含 front matter,比实际文件小约 140~160 字节;改为整文件大小(含 front matter),与门禁口径一致。
- **工具状态**:切分与校验脚本为一次性脚本 `.agent/tmp/split-g04.py`(未入库),支持 dry run(默认)与 `--write`,写入前必须先通过标题/行级守恒与链接深度回退校验;本次 18 个分册全部由该脚本生成,四次重跑均为「回滚原文 → dry run → write」的同一路径。

## 2026-09-14 维护者验收与收口

- 维护者确认切片 0–5 全部通过,批准收口并授权推进 G05。
- 收口动作:本 Task README 的 Current State 由「实施完成,待维护者验收」改为「已收口(2026-09-14,维护者验收确认)」;治理任务索引 `.agents/tasks/governance/README.md` 的 G04 行同步改为「已完成收口」;改动按切片提交本地 `master`(每份文档一个提交 + 一个记录/基线收口提交,保留逐份 revert 粒度),**未推送**。
- 提交前复核 `git fetch origin`:`origin/master` 与本地 `master` 均为 `e5d5d26`,无领先/落后,无需 `merge --ff-only`。
- 收口后未运行项不变:浏览器与真实来源验收(纯文档改动不适用)、CI(未推送)。
