---
parent: docs/requirements/0002-product-requirements.md
range: 分册勘误登记（追加型台账）
sealed_at: 2026-09-18
tags: [requirements, prd, errata]
tokens_est: 3848
---

# 分册勘误登记

分册封口后只读；对已封口分册的更正登记在此，不改写分册正文。

本台账从主文档 [`0002-product-requirements.md`](../0002-product-requirements.md) 拆出（2026-09-18），拆出原因是主文档逼近文档治理的警戒区（>9k token）。**只搬位置，不改写任何既有条目**；主文档保留索引链接。新增勘误继续追加到本表末尾。

按预算滚动归档（[`oversized-doc-splitting-v1`](../../proposals/oversized-doc-splitting-v1.md) §4.3）：更早的 20 条勘误已移入历史分册（只搬位置、只加一级相对链接前缀），**只切历史、不切当前口径**；本文件保留最近记录与仍生效的更正。

| 分册 | 覆盖范围 | 大小 |
| --- | --- | --- |
| [ERRATA/history-2026-09-15-20.md](ERRATA/history-2026-09-15-20.md) | 2026-09-15 ~ 2026-09-20（20 条） | 20.9 KB |

| 日期 | 位置 | 更正 | 理由与决策 |
| --- | --- | --- | --- |
| 2026-09-22 | [`part-07-1.md`](part-07-1.md) §7.1 的行 AUT-004 的阶段状态（仍标 `Phase 2`，此前无勘误） | AUT-004 仍记为 **Phase 2 未交付**，但 **v1 形态已裁定**：Phase 2 只交付 `webhook` 一种剩余形态（入口以 `TriggerBinding` 为定位对象、持有不可猜的不透明标识、凭证走 SecretStore、请求幂等、限流与体积上限、请求体脱敏，触发的原因/输入/时间/定义版本四项证据落 Run 既有位置）；`event`（内部事件）、`condition`（条件变化）、`dependency`（上游 Workflow 结果）三种形态记为 **Phase 3**；`poll` 不单列类型（轮询来源由 `schedule` + 持久 checkpoint 表达） | 架构 §4.1 早已把七种 Trigger 类型写进「第一版合同预留」（`manual`/`schedule`/`poll`/`webhook`/`event`/`condition`/`dependency`），需求表的四种形态正是其中四种，性质是**已冻结的架构合同未落地**、不是新增需求（与 AUT-010 同类）。依赖逐条核实：`dependency` 真依赖 Phase 3——它要求用户先有自己的 Workflow，而「用户自定义 Workflow 产品面」按 PRD 定级 Phase 3；`event`/`condition` 缺的是领域事件订阅分发与条件求值（事件表已在约 15 个领域写入，但只被读来做时间线/审计，没有订阅者），与 Phase 3 交付物没有合同依赖，但它们的价值峰值在 Knowledge Workflow 之后；`webhook` 的瓶颈既不是 Phase 3 也不是代码量，而是消费者——四个内置来源都只声明一个轮询 `fetch` 操作、仓库无任何 inbound 入口。维护者 2026-09-22 接受 Proposal [`trigger-forms-v1`](../../proposals/trigger-forms-v1.md) 的待裁定项 1–6，稳定决定见 ADR [`0024`](../../adr/0024-trigger-forms-v1.md)；v1 验收消费者取用户自己的自动化（本机脚本/工具调入口触发采集计划），等真实认证 Adapter 的平台推送后置。需求文字与验收条件不改写；实施记录复用 Task [`23`](../../../.agents/tasks/23-trigger-sdk/README.md)。 |
| 2026-09-22（同日补记） | [`part-07-1.md`](part-07-1.md) §7.1 的行 AUT-004 的交付状态（本表同日已记其 v1 形态裁定） | 记为 **Phase 2 已实现（webhook 形态）**：入口 `POST /hooks/collection-plans/{token}` 由外部自动化调用（带凭证与外部事件标识），触发采集计划，触发的原因/输入/时间/定义版本四项证据落在 Run 上；`event`／`condition`／`dependency` 三种形态仍记 **Phase 3** | 实现为 Task [`23`](../../../.agents/tasks/23-trigger-sdk/README.md) 的切片 3–7：触发类型与证据、一计划多触发器（ADR [`0025`](../../adr/0025-multi-trigger-per-plan.md)）、入口标识与凭证、inbound 端点、计划面板入口、真实消费者验收；代码在分支 `feat/t23-trigger-webhook`（**尚未合并 master**，合并需维护者授权）。验收证据四层且分别报告：focused 单元 119 文件／685 用例全绿、Node 进程 E2E 6 文件／11 用例全绿（含入口端点 5 例）、浏览器 E2E 29 用例全绿（含入口面板与「外部触发」Run 1 例）、真实来源 `bun run test:real:entry` 通过（真实外网 RSS 经入口触发，Run 成功、20 条条目、触发证据与投递事件标识一致）。需求文字与验收条件不改写。 |
| 2026-09-22（合并补记） | 本表上一条 AUT-004 交付记录里的「尚未合并 master」 | 已解除：分支 `feat/t23-trigger-webhook` 已 `--no-ff` 合入 `master`（合并提交 `da932cd`）；合并后重跑门禁全绿——typecheck、单元 119 文件／685 用例、Node 进程 E2E 6 文件／11 用例、浏览器 E2E 29 用例、`docs:check` 747 文件 0 失败、size 门禁 PASS、`git diff --check` 干净 | 只补记合并事实，不改写上一条的裁定与证据内容。 |
| 2026-09-23 | [`part-07-1.md`](part-07-1.md) §7.3 的行 ING-012 的阶段状态（本表 2026-09-20 行记为「部分交付、仍未闭合」） | 记为 **Phase 2 已交付**：验收条件里「状态可备份、恢复、迁移并按 Connection／Source 范围隔离」由本片补齐 | 缺口有两半：没有按命名空间的导出／恢复／迁移入口，且**归属不是数据**——命名空间是宿主按 manifest 模板算出来的字符串，库里没有归属记录，所以「按连接导出」必须每次绕道计划表 + 解析模板，「从命名空间反查归属」做不到。实现：新增 `ConnectorStateNamespace` 归属表（namespace 主键 + `planId`，migration `20260923120000_connector_state_namespace_owner` 建表并回填默认模板 `{id}` 的现状），宿主解析状态句柄时登记一次、`putState`／CAS 路径不变；导出／导入／清单三条路由（未归属抽屉默认不带走、只能点名；导入默认只补缺失、显式 `overwrite` 时 `version = 本地 + 1`；单抽屉可改名）与 Web 存储面板入口。导出件只含 `ConnectorState`；整库副本仍走 `POST /backups`，用户数据导出仍不含它。验证：单元 122 文件／698 用例、Node 进程 E2E 2/2、浏览器 E2E 1/1、组件实验室 14 passed、typecheck 0、`docs:check` 0 失败、size 门禁 PASS、路由守卫 3/3。决定见 Proposal [`connector-state-export-v1`](../../proposals/connector-state-export-v1.md) 与 ADR [`0026`](../../adr/0026-connector-state-export-v1.md)；记录见 Task [`22`](../../../.agents/tasks/22-connection-state-store/README.md) 追加切片 2。需求文字与验收条件不改写。 |
| 2026-09-23（合并补记） | 本表上一条 ING-012 记录的实现口径（当时按分支记录） | 已解除：分支已 `--no-ff` 合入 `master`（合并提交 `1b5cabc`）并推送；合并后重跑门禁——typecheck 0、单元 122 文件／698 用例、`docs:check` 755 文件 0 失败、size 门禁 PASS、路由守卫 3/3、`git diff --check` 干净 | 只补记合并事实。**合并后第一次跑门禁时 typecheck 与 5 个用例失败**，根因是主工作区的 Prisma 客户端仍是旧 schema 生成的（`connectorStateNamespace` 不存在），`bun run db:generate` 后全绿——schema 变更合并后必须先重生成客户端再跑门禁。 |
| 2026-09-23 | [`part-07-1.md`](part-07-1.md) §7.4 的行 LIB-004 的阶段列（仍标 `Phase 2`） | 已交付的部分记为 **Phase 2 已交付**；**Artifact 目标与正文片段字符级锚点改标 `Phase 3`** | 第四切片注记（2026-09-08）当时就写明「Annotation 目标 = Story/Entry/Topic，Artifact 与正文片段字符级锚点后置」；批注核心已由用户组织切片（Task 13）交付——`annotationSchema` 带 `targetRevisionId`（目标版本）、`actor`（作者）、`createdAt` 与可选 `quote`／`evidence`。此后 `PROJECT-STATUS.md` 与 [`Phase-2-UNDO.md`](../../../Phase-2-UNDO.md) 都按「延后到 Phase 3」记录，但需求表字面仍像 Phase 2 欠着——本行登记该改标。**未做的两半**：批注目标不支持 Artifact；`quote` 目前只是文本快照，不能定位到 revision 正文的具体区间。需求文字与验收条件不改写。 |
| 2026-09-23 | [`part-07-1.md`](part-07-1.md) §7.1 的行 AUT-009 的交付状态（此前无勘误；[`Phase-2-UNDO.md`](../../../Phase-2-UNDO.md) 的 2026-09-20 复核才把它的可见性半边登记为缺口） | 记为 **Phase 2 已交付** | 四条验收条件逐条核对：①可创建可复用连接、多个来源／采集计划引用同一连接——Task 22 交付连接对象与 API，Task 33（`4ef3636`）在同一连接下建出两个 Bilibili 计划；②**用户能看到连接状态、授权范围和失效原因**——由 Task [`22`](../../../.agents/tasks/22-connection-state-store/README.md) 追加切片 3 补齐（`--no-ff` 合并 `2cfe379`），面板显示并记录授权范围（要求合法 JSON）与失效原因；③撤销凭证不删除已录入历史——`deleteConnection` 只把来源 `connectionId` 置空、删连接行与 SecretStore 里的密钥字节，不触碰 Entry／Observation；④普通配置／Job payload／日志不含凭证明文——凭证只在 SecretStore，公开面只出不透明 `SecretRef`。这两个字段今天没有自动写入方（真实认证 Adapter 属 EXT-006 的后续），面板提供的是用户侧记录入口，见该分册的 AUT-009 注记。需求文字与验收条件不改写。 |
| 2026-09-23 | [`part-07-1.md`](part-07-1.md) §7.1 的行 AUT-010 的交付状态（本表 2026-09-20 行只记到「已裁定并进入实施」） | 记为 **Phase 2 已交付** | 由 Task [`33`](../../../.agents/tasks/33-collection-plan/README.md) 交付并 `--no-ff` 合并 `4ef3636`：`CollectionPlan` 落地，按计划隔离连接、频率、媒体预算与失败状态（`CollectionPlanSnapshot` 的 `connectionId`／`scheduleIntervalMs`／`mediaPolicy`／`lastError`）、checkpoint（按计划 id 寻址）与状态命名空间；产品面按连接分组，真实验收在同一连接下同时跑通 hot（每 2 小时）与 feed（每 30 分钟）两个计划、各 20 items，Run／错误／游标按计划分开。**逐条对照需求文字时的两处如实说明**：①「每个计划拥有自己的来源操作」由「v1 计划与采集目标一对一」承接——`operationId` 仍在来源上，计划不另存；②迁移第 4 步 contract（从来源移除旧列）单独排期，不影响本行验收。决定见 ADR [`0023`](../../adr/0023-collection-plan-v1.md)。 |
| 2026-09-23 | [`part-07-3.md`](part-07-3.md) §7.11 的行 EXT-007 的交付状态（此前无勘误） | 记为 **Phase 2 已交付** | 逐条核对：①**声明面**由 Task 23 交付（ADR-0018）——manifest 声明 operation 的输入/输出 schema、稳定 external key、discovery context、媒体状态、`auth` 与 `stateStoreNamespace`；②「Adapter 不自行持久化 Secret 或核心领域状态」——`plugins/` 对 Prisma 与 SecretStore 零引用，连接器只返回条目、状态经宿主注入的句柄读写；③「Cosmos 可以校验能力、版本、预算和恢复语义」——能力：宿主按 `connector.capabilities.includes(mediaDownloadCapability)` 决定是否进入媒体获取；版本：版本化 `sourceDefinitionRef` 加 operation 归属校验（`manifest.operationIds.includes(operationId)`）与版本化 config schema；预算：媒体预算在受控点强制并落 `budget_file`／`budget_run`；恢复语义：`ConnectorExecutionError.retryable` 与 Action 的 `retryableErrors` 允许清单（`dependency_unavailable`／`timeout`／`rate_limited`）；④「通过同一合同支持多个采集计划」由 2026-09-22 的采集计划 v1（`4ef3636`）兑现。需求文字与验收条件不改写。 |
| 2026-09-23 | [`part-10-12.md`](part-10-12.md) §12 的 Phase 2 范围列举（此前无勘误） | 记为**口径注记、不改写正文**：§12 的 Phase 2 范围只列 Label/Annotation/Collection/Saved View、Story/Topic/Entity、Board/Section/Block，**不含平台面**（Connection／Secret／State、Trigger、连接与认证）；平台面按 §7 的逐行阶段计入 Phase 2 | Proposal [`connection-login-lifecycle-v1`](../../proposals/connection-login-lifecycle-v1.md)（accepted，2026-09-23）的待裁定项 1：`Phase-2-UNDO.md`、`PROJECT-STATUS.md` 与 2026-09-23 的多条勘误一直按 §7 的逐行阶段核 Phase 2 欠条，所以这条注记是**记录落后于既成事实**而不是新增需求；分册封口后只读，故不改 §12 正文。需求文字与验收条件不改写。 |
| 2026-09-23 | [`part-07-3.md`](part-07-3.md) §7.11 的行 EXT-006 的阶段状态（本表同日既有 EXT-006 行记为「仍为部分交付」，本行收口） | 记为 **Phase 2 已交付**：剩下的半边——**多 operation 的真实消费者**与「按 operation 声明配置 schema」——由 Task [`23`](../../../.agents/tasks/23-trigger-sdk/README.md) 的切片 8（Proposal [`connection-login-lifecycle-v1`](../../proposals/connection-login-lifecycle-v1.md) 决定 3，即该 Proposal 的「切片 2」）补齐 | 两条验收逐条核对：①「Web/API 可以根据声明展示配置」——`sourceOperationManifestSchema` 增可空 `configurationSchema`（`null` = 沿用定义级那份），canonical 校验按 `(sourceDefinitionRef, operationId)` 检索（`getSourceConfigurationSchema(ref, operationId)`，未登记回退定义级），Web 的来源定义选择器旁多了操作选择器，字段按所选操作渲染、保存与探测都提交所选 `operationId`；②「按声明展示登录状态」由切片 4a/4b 交付（`auth.probeSupported` 驱动探测入口，结论写回连接的 `status`/`account`/`lastError`/`lastCheckedAt`）；③「增加新 Adapter 不需要修改核心数据库表或 Worker 的专用分支」——Bilibili 的第二个 operation（`search`）只改 `catalog.ts` 的声明、canonical schema 注册表、Adapter 自己的 `operationId` 分派（`plugins/collectors`）与 Web 渲染，宿主侧零专用分支，本切片**没有**数据库迁移、没有新路由。验证：typecheck 0、单元 124 文件／725 用例全绿、组件实验室 E2E 19 passed、浏览器产品 E2E 全套件 33 passed（首次全套件运行暴露了 1–2 例既有脆弱点——固定宽度侧栏里的连接面板不收缩而画到计划列表上，挡住计划行的按钮；已同批修掉，修法是把 `min-w-0` 铺到那条 flex/grid 链上，修前修后各有量测与截图证据）、`docs:check` 760 文件 0 失败、size 门禁 PASS、`git diff --check` 干净。实现记录见 Task 22 的切片 4a／4b 与 Task 23 的切片 8；分支 `feat/t22-ext-006-login-lifecycle`（切片 4a `bd9a9c0`、4b `94c6dec` 已提交，切片 8 未提交）尚未合并 master。需求文字与验收条件不改写。 |
| 2026-09-23 | [`part-07-3.md`](part-07-3.md) §7.11 的行 EXT-006 的阶段状态（本表 2026-09-20 行记为「部分交付、仍未闭合」） | 记为 **仍为部分交付**，但缺口只剩一半：「Web/API 按声明展示配置**和登录状态**」的**登录状态半边已交付**，**多 operation 的真实消费者**仍待 Task 23 的切片 2 | 登录状态此前没有真相源（面板的授权范围与失效原因只能用户手填）。交付分两片：**4a** 让连接成为登录态载体——`ConnectionInstance.configJson` 装 OpenCLI profile，来源配置不再带它（三段式迁移：加列、按 profile 建连接并补计划绑定、从 live 来源配置移除；`20260923180000_connection_adapter_config`），执行快照增可选 `connection` 投影并**随入队冻结**（AUT-016 点名 Connection），`feed` 的登录态校验改由连接器读连接投影完成；**4b** 让登录状态有自动写入方——manifest 的 `auth.probeSupported` 声明驱动探测入口（API 与 Web 都按声明判断）、`connection-probe` Job 由 Worker 执行、结论写回 `status`/`account`/`lastError`/`lastCheckedAt`（`20260923190000_connection_last_checked_at`）。验证：全量单元 124 文件／720 用例、组件实验室 E2E 4 passed、浏览器产品 E2E 2 passed（真实栈探测往返，本机一次探测约 52s）、typecheck 0、`docs:check` 759 文件 0 失败、size 门禁 PASS。稳定决定见 ADR [`0027`](../../adr/0027-connection-login-lifecycle-v1.md)（部分取代 ADR-0017 决策 1）与 Proposal [`connection-login-lifecycle-v1`](../../proposals/connection-login-lifecycle-v1.md)（accepted）；实现记录见 Task [`22`](../../../.agents/tasks/22-connection-state-store/README.md) 的切片 4a／4b（**分支 `feat/t22-ext-006-login-lifecycle` 已提交切片 4a `bd9a9c0` 与切片 4b，尚未合并 master**）。需求文字与验收条件不改写。 |
