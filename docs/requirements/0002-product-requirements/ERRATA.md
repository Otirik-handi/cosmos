---
parent: docs/requirements/0002-product-requirements.md
range: 分册勘误登记（追加型台账）
sealed_at: 2026-09-18
tags: [requirements, prd, errata]
tokens_est: 5200
---

# 分册勘误登记

分册封口后只读；对已封口分册的更正登记在此，不改写分册正文。

本台账从主文档 [`0002-product-requirements.md`](../0002-product-requirements.md) 拆出（2026-09-18），拆出原因是主文档逼近文档治理的警戒区（>9k token）。**只搬位置，不改写任何既有条目**；主文档保留索引链接。新增勘误继续追加到本表末尾。

| 日期 | 位置 | 更正 | 理由与决策 |
| --- | --- | --- | --- |
| 2026-09-15 | [`part-07-2.md`](part-07-2.md) §7.5 ORG-021 的「阶段」列 | 由 `Phase 2` 更正为 `Phase 3` | ORG-021 要求 Entry → Story 的组织允许确定性算法、传统模型与 LLM 协同，并提出分类、聚类、实体、关系、重要性和紧急性建议；其落地依赖 Agent 调用边界、Agent 候选 Revision 与确认策略、第一版预算模型，而这三者都是 Phase 3 的交付物，§12「Phase 3：Agent Artifact 与 Workspace」的范围也已含 Knowledge Workflow。原表将 ORG-021 标为 `Phase 2`，与 §7.5 各切片注记一致记的「自动聚类与 Knowledge Workflow（ORG-021）后置」自相矛盾，并使 Phase 2 按需求表字面无法完成；该矛盾此前未暴露，是因为 Phase 2 的四条验收标准（§12）不要求自动聚类。维护者 2026-09-15 裁定改标 Phase 3；ORG-021 的需求文字、验收条件与既有切片注记均不改写。 |
| 2026-09-15 | [`part-07-2.md`](part-07-2.md) §7.5 ORG-014/020 的切片注记（2026-09-09）中「用户状态的显式迁移与撤销后置」 | 该项已实现，不再是后置项：见 ADR [`0020`](../../adr/0020-story-split-user-state-migration-v1.md) 与主文档 §13 待决定事项 10 的注记 | ADR-0012 的 Revisit Gate 首项被触发并已处理。ADR-0020 固定：只迁移以 Story 为 target 的用户状态、迁移是独立命令、撤销 = 同命令反向调用、作用域限定同一 split 家族、冲突按唯一键让目标侧优先；迁移是历史壳上唯一允许的写操作。分册正文与既有注记不改写。 |
| 2026-09-16 | §7.4 的行 AUT-005、ING-013、LIB-005（[`part-07-1.md`](part-07-1.md)）、§7.5 的行 REC-008（[`part-07-2.md`](part-07-2.md)）、§7.6 的行 BRD-004（[`part-07-3.md`](part-07-3.md)）的「阶段」列 | AUT-005、ING-013 由 `Phase 2` 更正为 `Phase 3`；LIB-005、REC-008、BRD-004 由 `Phase 2` 更正为 `Phase 4` | 与 ORG-021 同类问题：这五条的**验收条件本身**依赖后续阶段的交付物，把它们留在 `Phase 2` 会让 Phase 2 按需求表字面无法完成。AUT-005 的自定义 Trigger/Action 插件运行时需要扩展 SDK 与插件运行时（Task 23 已记为 Phase 3）；ING-013 的可配置 Entry → Story 知识处理依赖 Knowledge Workflow（Phase 3）；LIB-005 的「未读」条件依赖 Read State（REC-015，Phase 4）；REC-008 的相关内容推荐属推荐体系（Phase 4）；BRD-004 的「系统按 policy 推荐 Spotlight」由 REC-014（Phase 4）承载（v1 已交付的人工固定部分不受影响）。需求文字、验收条件与既有的切片排序注记均不改写。改标后 §7 中仍标 `Phase 2` 而未交付的是**真正属于 Phase 2 的尾巴**：AUT-004（webhook/内部事件等触发形态）、AUT-010（一个连接下的多采集计划）、BRD-006（Feed Block 的查询条件绑定）、BRD-007（多来源差异与用户操作）、LIB-004（正文片段字符级锚点）、LIB-008（导出）、ING-009（余项按 ADR-0015 Revisit Gate）；Phase 2 因此不再自相矛盾，而是明确留有开放项。维护者 2026-09-16 指令「改标」。 |
| 2026-09-18 | 本表 2026-09-16 行里「BRD-006（Feed Block 的查询条件绑定）」属于未交付尾巴的表述 | BRD-006 由「未交付」改为「部分交付」：Feed Block 绑定 Saved View 已实现，缺的是验收条件里的「排序配置」 | 2026-09-18 复核发现该表述与实现证据冲突：ADR-0010 与架构 §5/§7 的看板 v1 注记都写「Feed Block 绑定 Saved View（BRD-006）在本片落地」；Task 14 的追加切片 `14ce892` 让阅读流区块按 `savedViewId` 独立取数并有浏览器 E2E；`apps/web/src/components/cosmos/board-view.tsx` 消费该绑定。BRD-006 的验收条件还要求「开发、硬件、娱乐等分区可以拥有不同来源与排序配置」，而 `SavedViewConditions`（`packages/contracts/src/user-organization.ts`）只有关键词/来源/时间/分类/Topic 条件、没有排序字段，所以按完整验收条件只能记为部分交付。维护者 2026-09-18 裁定改口径；需求文字、验收条件与既有切片注记均不改写。 |
| 2026-09-18 | [`part-07-2.md`](part-07-2.md) §7.5 ORG-003、ORG-004、ORG-022 的「阶段」列 | 三条的**自动半边**（自动识别、自动归并、成员候选/接受/拒绝）随 ORG-021 归 `Phase 3`；**人工半边**（手动 Entity 与关系、人工 merge/split/成员修正、证据关系）记为 Phase 2 已交付 | 与 ORG-021 同一理由（该行已于 2026-09-15 改标）：自动路径依赖 Knowledge Workflow、Agent 候选 Revision 与确认策略、第一版预算，三者都是 Phase 3 交付物；三条的需求文字与验收条件不改写。维护者 2026-09-18 采纳。 |
| 2026-09-18 | [`part-07-3.md`](part-07-3.md) §7.8 BRD-006 的「阶段」列 | 绑定 Saved View 记为 Phase 2 已交付；验收条件里的「排序配置」归 `Phase 4` | 绑定已随看板切片落地（ADR-0010、Task 14 的 `14ce892`，Web 按 `savedViewId` 独立取数并有浏览器 E2E）；`SavedViewConditions` 只有关键词/来源/时间/分类/Topic 条件、没有排序字段，而排序属 Phase 4 推荐体系（与 REC-008/REC-014 同批）。维护者 2026-09-18 裁定。 |
| 2026-09-18 | [`part-07-3.md`](part-07-3.md) §7.8 BRD-007 的「阶段」列 | 多来源、时间线、相关内容记为 Phase 2 已交付；「差异」对比与 Agent 产物归 `Phase 3` | 前两项已由 Task 15 交付并有浏览器 E2E；「差异」对比与 Agent 产物都落在深入页重做范围（[`ui-surface-ownership-v1`](../../proposals/ui-surface-ownership-v1.md) 已 accepted、实现待重做），且 Agent 产物本身是 Phase 3 对象。维护者 2026-09-18 裁定。 |
| 2026-09-18 | [`part-07-1.md`](part-07-1.md) §7.3 ING-009 的「阶段」列 | 按来源的媒体策略（图片开关、单文件/单次预算、保留期、失败重试）记为 Phase 2 已交付；剩余五项逐项定归属：历史媒体回填**冻结（不做）**、音频/视频下载实体→`Phase 4`、单条目媒体数量上限→`Phase 3`、全局默认值 env 化→`Phase 3`、媒体类型配置维持 v1 的图片范围 | 前四项已由 ADR-0014/0015 与 Task 19/20 交付；历史回填与 ADR-0005「无历史回填」一致，冻结不改变既有合同；音视频实体属渠道广度（Phase 4）；数量上限与 env 化属媒体策略精细化（Phase 3）。维护者 2026-09-18 裁定。 |
| 2026-09-18 | [`part-07-2.md`](part-07-2.md) §7.5 ORG-015 的「阶段」列 | 判为 Phase 2 已满足；并注明 v1 由标签与看板承担 Topic 组织角色，Topic↔Topic 类型化关系后置 | 需求文字是「用带类型的 Relation、标签**或** Workspace/Board 组织」——三支取其一即可；验收条件只要求「不把展示层级误当成父子关系」，v1 不建层级已满足。维护者 2026-09-18 裁定按宽松读法。 |
| 2026-09-18 | [`part-07-1.md`](part-07-1.md) §7.3 ING-008 的验收条件 | 验收里的「断网后可查看已保存图片、**音频或视频**」按 ADR-0005 已冻结的媒体边界收窄为：图片下载为本地实体，音频/视频与其它类型只保存元数据与原文外链；ING-008 记为 Phase 1 已交付 | 验收文字写于媒体边界冻结之前；ADR-0005（[`media-boundary-v1`](../../proposals/media-boundary-v1.md) accepted）已把 v1 定为「图片实体 + 音视频元数据」，音视频下载实体在 ING-009 的剩余项里改标 Phase 4。维护者 2026-09-18 在 Phase 1 收口计划里裁定走勘误而不实现音视频实体。需求文字与其它验收条件不改写。 |
| 2026-09-20 | [`part-07-1.md`](part-07-1.md) §7.3 的行 ING-012 的阶段状态（仍标 `Phase 2`，此前无勘误） | 记为 **部分交付、仍未闭合**：命名空间化 + 版本化 StateStore 已交付且已被内置连接器消费；验收条件里的「状态可备份、恢复、迁移」未实现 | 已交付：`PrismaConnectorStateStore`（`packages/storage-prisma/src/connector-state-store.ts`，`ConnectorState(namespace, key, valueJson, version)` + 版本 CAS）、`ConnectorStatePort`/`ConnectorStateHandle` 合同、宿主按 manifest 的 `stateStoreNamespace` 解析命名空间（`apps/worker/src/main.ts`），RSS 连接器已实际用它保存 ETag/Last-Modified（AUT-003 切片）。未闭合：验收条件要求「状态可备份、恢复、迁移并按 Connection/Source/Workflow 范围隔离」——现有备份只复制 SQLite 文件（OPS-004 的 `POST /backups` 走 `VACUUM INTO`），没有按命名空间导出/恢复/迁移的入口，也没有范围隔离的验收。`PROJECT-STATUS.md` 已把 ING-012 列在「仍标 Phase 2 而未闭合」清单里，本条只是把它登记进需求表口径。 |
| 2026-09-20 | [`part-07-3.md`](part-07-3.md) §7.10 的行 OPS-004 的阶段状态（仍标 `Phase 2`，此前无勘误） | 记为 **部分交付、仍未闭合**：备份、恢复、清理入口已交付；**导出未实现**（全仓没有 export 路由） | 已交付（Task 24，ADR-0019）：`GET /storage-stats`（raw/user/rebuildable/cleanable 分层统计）、`POST/GET /backups`（`VACUUM INTO` 复制 SQLite，不依赖源码 checkout）、`POST /backups/:id/restores`（覆盖 + 恢复前保护）、清理沿用 media-cleanup（先预览再确认）。未闭合：需求文字是「备份、恢复、**导出**和清理入口」，导出没有对应实现，`PROJECT-STATUS.md` 也把「导出/Artifact 清理/Blob 备份后置」记在 Task 24 的边界里。 |
| 2026-09-20 | [`part-07-3.md`](part-07-3.md) §7.11 的行 EXT-006 的阶段状态（仍标 `Phase 2`，此前无勘误） | 记为 **部分交付、仍未闭合**：manifest 的**声明面**已交付；四个内置 manifest 目前都只声明单个 `fetch` operation，且没有真实认证 Adapter，验收条件里的「Web/API 按声明展示配置和登录状态」只有配置半边 | 已交付（Task 23，ADR-0018）：`SourceDefinitionManifest` 扩展 `auth`（`none|oauth|cookie|secret_ref|external` + `secretRefRequired`）与 `operations`（operationId、input/output schema、external key、discovery context、media、stateStore 命名空间），`operationIds` 是数组、校验按 `operationIds.includes(operationId)`，结构上支持多 operation。未闭合：`createBuiltinManifestCatalog` 的四个 manifest 都是 `operationIds: ["fetch"]`（多操作能力没有使用方，也没有对应用例），`auth.kind` 只有 `none`/`external`，登录状态展示要等真实认证 Adapter；`PROJECT-STATUS.md` 已记「EXT-006 的『登录状态展示』要等真实认证 Adapter」。 |
| 2026-09-18 | [`part-07-1.md`](part-07-1.md) §7.2 的行 RUN-010、RUN-011（[`part-07-3.md`](part-07-3.md) §7.10 的行 OPS-010）的「阶段」列 | 三行由 `Phase 1C` 更正为 **`Phase 3`**，与插件运行时、Agent 执行位置同批 | 维护者 2026-09-18 先裁定把三行排除出 Phase 1 收口、单独排期，同日进一步裁定目标阶段为 `Phase 3`。理由：三行要求的是远程 Worker Gateway（Session、long-poll claim、Receipt、Direct/Gateway conformance 与三面责任边界），它服务的是「无数据库权限的远程 Worker 执行可移植 Action」，与 Phase 3 的插件运行时和 Agent 执行位置（`remote_worker` placement）是同一个故事；仓库内当前没有任何 Gateway 代码，`docs/spec/README.md` 与 `docs/api/0005-worker-gateway-api.md` 都只把它记为边界设计。改标后 §7 不再把这三行读作 Phase 1 的待交付项；需求文字与验收条件不改写。 |
| 2026-09-18 | [`part-07-3.md`](part-07-3.md) §7.11 的行 EXT-008 的「阶段」列 | 记为 **Phase 1 已交付**：验收条件①没有 Connector executable 时 Catalog Query 可用、②Worker 注册精确 manifest evidence 已有行为测试；③「独立构建/部署实跑」并入既有 Phase 1 后置债的「manifest-only API、executable-only Worker 与独立 Migrator 完整生产验收」一条，不在本行单独计 | 实现证据：`apps/api/src/app.controller/sources.ts` 的 `GET /source-definitions` 与 `/connectors` 只读静态 manifest catalog（`packages/application/src/catalog.ts` 的 `createBuiltinManifestCatalog`），不加载也不执行 Connector；`apps/api/package.json` 依赖里没有 `@cosmos/plugin-collectors`，可执行 Connector 只在 `apps/worker/src/main.ts` 组合根构造的 `ConnectorRegistry` 里。行为测试：`packages/application/src/catalog-manifest-only.test.ts` 断言四个内置来源的 manifest/schema/capability 在无 executable 时全部可得、connector 目录由同一批 manifest 投影、catalog 无执行面、API 依赖不含 plugin-collectors、执行必须显式注册 executable。维护者 2026-09-18 裁定按此收口：独立构建实跑属部署验收，与 Docker/独立 Migrator 同批。 |
| 2026-09-18 | [`part-07-1.md`](part-07-1.md) §7.1 的行 AUT-001 的验收条件「删除凭据、停用来源和删除历史数据是三个独立动作」 | 三项全部记为 **Phase 1 已交付**：「删除凭据」由维护者 2026-09-18 裁定补实现——删除 Connection 时连带删除 SecretStore 中的密钥字节 | 此前状态：`SecretStorePort.delete` 与 `FileSecretStore` 存在但无调用方，`POST /connections/:connectionId/removals` 只把来源 `connectionId` 置空并删 Connection 行。实现（2026-09-18）：`PrismaCosmosRepositoryBase` 注入 `SecretStorePort`（缺省按 Data Root 的 `secretRoot` 构造 `FileSecretStore`，与 Blob 同构），`deleteConnection` 先读连接的 `secretRef`、删行与解引用后再删密钥字节；顺序是「先删行、后删密钥」，密钥删除失败只记 `connection.secret.delete_failed` warn 而不让删除失败。行为测试：`packages/storage-prisma/src/connection-state-store.test.ts` 断言删除连接后 `secrets.read(secretRef)` 返回 null，以及无 `secretRef` 的连接删除保持幂等。**同批修掉一个潜在缺陷**：`FileSecretStore` 的路径校验原先「含 `:` 即判为逃逸」，而产品侧写入的 ref 形态是 `secret:conn-1`，导致任何真实凭据都写不进去；现改由 `isAbsolute` 与「解析后仍在根内」判断逃逸（`../`、绝对路径、空 ref 仍被拒），`packages/storage-prisma/src/secret-store.test.ts` 覆盖新形态与三种拒绝。需求文字与其它验收条件不改写。 |
| 2026-09-18（2026-09-20 补记） | [`part-10-12.md`](part-10-12.md) §12「Phase 1：信息录入与离线查询」的前言、范围、验收，以及第 96 行「当前不应从现有 fixture/RSS 管线基线推断…已完成」整段 | 逐项定状态，不改写正文：**已交付**——配置入口与真实 RSS 产品 E2E、Source 删除（墓碑）、媒体/搜索字段、OPS-002 的 Run→Job 产品面；**按维护者 2026-09-07 划线后置、不作为 Phase 1 阻塞**——Docker/Compose 容器验收、发布部署、真实公网长时定时抓取（真实单次与双源验收已过，缺长时间稳定性）、非 Windows 平台 smoke、长时间故障恢复演练；**不属 Phase 1 范围**——用户自定义 Workflow 产品面（PRD 定级 Phase 3）、Connection/Secret/State（PRD 定级 Phase 2，Task 22 已交付地基）、多采集计划（Phase 2，未交付）；**按需求原文属可选或已收窄口径**——Step 投影（RUN-001 写明「命名 Step 投影可选」，Attempt 明细保持 API-only）、完整 Run 管理/运维（RUN-004 属 Phase 2；OPS-002 的「预算」已收窄为媒体预算，Run 级时间/token 预算随 RUN-006/RUN-009 后置） | 该段验收文字写于 2026-08 至 09-02 的切片期间，此后多项已交付或已划线，但正文一直没有对应勘误，使 §12 读起来像 Phase 1 还欠配置入口、Source 删除与真实 RSS 产品 E2E。逐项证据：配置入口与真实 RSS 产品 E2E 见 Task 02 的配置优先产品 E2E 切片；Source 删除见 Task [`32`](../../../.agents/tasks/32-phase1-closure/README.md) 切片 4（`20260918120000_source_soft_delete_v1`，墓碑保留历史）；媒体/搜索字段见 Task 32 切片 1–3 与 ING-008 勘误；Run→Job 产品面见 Task 32 切片 5（`JobSnapshot` 带 `attempts`/`maxAttempts` 与错误码，`GET /runs/:runId/jobs`）。第 96 行那段列出的每一项都按上表归类，其中 Connection/Secret/State 与多采集计划本就不属 Phase 1，Step API 与完整 Run 管理按需求原文可选或属 Phase 2。后置债的划线原文见 [`PROJECT-STATUS/history-2026-09-phase1.md`](../../../PROJECT-STATUS/history-2026-09-phase1.md) 的 2026-09-07 节。维护者 2026-09-18 复核采纳。 |
